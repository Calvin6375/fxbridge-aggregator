name: fxbridge_aggregator
description: Multi-Source Rate Aggregator API for forex and crypto exchange rates
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.7
  http: ^1.2.0
  dotenv: ^5.0.0
  shelf_logger: ^1.0.0

dev_dependencies:
  test: ^1.24.0
  build_runner: ^2.4.7

